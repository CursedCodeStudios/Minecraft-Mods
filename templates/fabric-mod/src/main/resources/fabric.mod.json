{
  "schemaVersion": 1,
  "id": "replace_me",
  "version": "${version}",
  "name": "Replace Me",
  "description": "Describe your mod here.",
  "environment": "client",
  "entrypoints": { "client": ["dev.minecraftsuite.replaceme.ReplaceMeClient"] },
  "license": "MIT",
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}",
    "java": ">=${java_version}",
    "fabric-api": ">=${fabric_api_version}"
  }
}
