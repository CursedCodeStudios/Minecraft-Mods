{
  "schemaVersion": 1,
  "id": "nautilusscout",
  "version": "${version}",
  "name": "Nautilus Scout",
  "description": "Highlights drowned holding a nautilus shell in either hand with a client-side glowing outline.",
  "environment": "client",
  "mixins": ["nautilusscout.mixins.json"],
  "license": "MIT",
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}",
    "java": ">=${java_version}",
    "fabric-api": ">=${fabric_api_version}"
  }
}
